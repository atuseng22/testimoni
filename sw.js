// sw.js removed in branch remove-testimoni
// This is a placeholder file indicating that the service worker was removed on this branch.
// Upload the new sw.js (the replacement service worker) to replace this placeholder before merging.
// Note: after the new sw.js is deployed to the default branch and served to users, ask users to clear site data on their phones (Chrome → Settings → Privacy and security → Clear browsing data → check "Cookies and site data" → Clear data) before reopening the site.
